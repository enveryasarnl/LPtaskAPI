stages:
  - build
  - test

maven-build:
  stage: build
  script:
    - "mvn compile"
test:
  stage: test
  script:
    - "mvn verify"
  artifacts:
    when: always
    reports:
      junit:
        - target/surefire-reports/TEST-*.xml