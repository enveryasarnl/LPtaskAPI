image: maven:3-jdk-8
test:
  stage: test
  script:
    - "mvn test site"
  artifacts:
    when: always
    paths:
      - target/surefire-reports/TEST-*.xml
      - target/site/index.html
    reports:
      junit:
        - target/surefire-reports/TEST-*.xml
        - target/site/index.html