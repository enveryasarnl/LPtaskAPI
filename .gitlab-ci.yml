image: maven:3-jdk-8
stages:
  - build
  - test

build:
  stage: build
  script:
    - "mvn compile"
test:
  stage: test
  script:
    - "mvn verify"
  artifacts:
    when: always
    reports:
      junit:
        - target/surefire-reports/TEST-*.xml