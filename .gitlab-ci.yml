image: maven:3-jdk-8
test:
  stage: test
  script:
    - "mvn verify"
  artifacts:
    when: always
    paths:
      - target/surefire-reports/TEST-*.xml
      - target/site/index.html
    reports:
      junit:
        - target/surefire-reports/TEST-*.xml
        - target/site/index.html